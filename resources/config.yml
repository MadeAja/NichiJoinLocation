#plugin make by MadeAja

#prefix join
#replace {player} to show player name
#replace {city} to show city name
#replace {region} to show region name
#colour use §
prefix: "[{region} {city}] {player} join in game"
